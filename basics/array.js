//Array 

const myArr = [0,1,2,3,5,10]
const myHeroes = ["shaktiman", "doga", "naagraj"]
const myArr2 =  new Array(1,2,3,4,5)
//shalow copy and deep
/* myarr.push(6)
myarr.pop()-remove last element

myArr.unshift(9) element is pushed from the intial of the array

my.arr.includes(9)  includes is used to compare the integer to the integer present in the array

const newarray= myarr.join() convert the array into the string 
slice (does not manipulate the original array), splice - divide array into two part (manipulate the original array)
*/


/* there are diffrent function such as
array.isArray
array from ("Anupriya")
array.from()*/
